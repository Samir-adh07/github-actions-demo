name: Hello From Samir's Project

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print Hello
        run: echo "👋 Hey Samir, GitHub Actions is running on your project!"
